# HOLODESK DEPENDENCIES
# Install all: pip install -r requirement.txt

# --- Core UI & Vision ---
pygame==2.5.2           # overlay window, drawing, game loop
opencv-python==4.9.0.80 # webcam capture, frame processing
mediapipe               # Google hand tracking (21 landmarks)
numpy==1.26.4           # array math used by opencv + gesture math

# --- Voice ---
SpeechRecognition       # microphone audio capture
faster-whisper          # local Whisper STT (no API cost, works offline)
pyaudio                 # low-level microphone access
comtypes                # Windows SAPI text-to-speech (reliable)
pyttsx3                 # TTS fallback

# --- AI & APIs ---
groq                    # Groq API client (Llama 3 — fast, cheap)
openai                  # GPT-4o Vision for screen analysis (Step 7)
moonshine-voice         # optional on-device STT fallback (requires model download)
rapidfuzz               # entity resolution for app/site names in voice commands

# --- Desktop Control ---
pyautogui               # scroll, hotkeys, screenshot capture
Pillow                  # image resizing before API calls (saves cost)
pyperclip               # clipboard read/write for automation + copy answers
requests                # web calls for future tool integrations

# --- Storage & Memory ---
psutil                  # read active process name for memory logger

# --- Windows System ---
pywin32                 # win32gui/win32api — transparency, always-on-top, window titles

# --- Utilities ---
python-dotenv           # loads API keys from .env file
