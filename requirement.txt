# HOLODESK DEPENDENCIES 
pygame==2.5.2   # open windows, draw shapes, handles game loop 
opencv-python==4.9.0.80 #reads webcam, processes images
mediapipe # Google's hand tracking 
numpy==1.26.4 # Math operations on arrays (used by opencv)